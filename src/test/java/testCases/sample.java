package testCases;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import java.util.ArrayList;
import java.util.List;

public class sample {
    public static void main(String[] args) {
        // Set path to your ChromeDriver
        //System.setProperty("webdriver.chrome.driver", "path/to/chromedriver.exe");

        // Create WebDriver instance
        

        
            // Open W3Schools HTML Tables page directly

            // Locate the table with id="customers"

            // Get all rows except the header

            // Store company names in a list

                // Get first cell in each row which contains the company name
            

            // Print extracted company names
            

            // Verify if specific company exists
            
            // Close the browser
        
    }
}

